# Consumer feedback and fix loop

| Finding                                                       | Reproduction / expected behavior                                                                                                                                                                              | Fix and verification                                                                                                                                                                                                                              |
| ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Content-control locks could not be cleared                    | Contract app locks a template control, then sets both flags false. Unlock previously raised GeneralException.                                                                                                 | Canonical lock-only writes may change the control's own flags while retaining ancestor protection. Server/browser contract app and nested-lock regression pass.                                                                                   |
| Duplicated error target for control locks                     | A refused `cannotEdit` write repeated the entire object path in its target.                                                                                                                                   | Pass the member name to the model command helper. Consumer rerun verified the corrected path.                                                                                                                                                     |
| Table collection properties lost loaded state                 | Load `row.cells` or `body.tables`, sync, then revisit the property and read `items`. It previously constructed a new unloaded collection.                                                                     | Cache Table.rows, TableRow.cells, Body.tables and Range.tables. Both consumer getter probes and browser save/reopen pass. Fields/pictures retain their existing caches.                                                                           |
| Font formatting after page-field insertion failed             | Report footer contains PAGE and NUMPAGES, then applies font settings.                                                                                                                                         | Canonical run splitting no longer groups field atoms with no owning run as a fictitious run. Instruction/chrome preservation and both-host regressions pass; unchanged report actions now pass.                                                   |
| Unsupported browser structural edits could escape UI tracking | Independent source review found new structural ops and package-only edits pass through attribution unchanged. The first runtime probe used the wrong public mode spelling; corrected probes use `suggesting`. | Host admission now checks the UI's suggesting state before planning/resource writes. List/table/field/control edits refuse NotSupported and preserve saved bytes. Existing supported browser text replacement remains available.                  |
| Table deletions orphaned comment/note records                 | Delete a row with a comment; API items disappeared while comments.xml retained the record.                                                                                                                    | `authorTable` participates in package cleanup gates, including notes. Six server/browser row/column/table tests verify deleted records, survivors, undo/redo and save/reopen. Independent original repro confirms closure.                        |
| Report table overflow and inherited numbering                 | Adding a column copies an edge width; inserting after a numbered list inherits paragraph formatting. The first report exceeded its body width and numbered headings/captions.                                 | App now explicitly sizes all columns to the 504-point body and detaches intended non-list paragraphs. This expresses document intent using public APIs; no output XML patch. Revised native Word inspection confirms both fixes; see `review.md`. |

| Aliases lost independent lock updates | Two proxies cleared different lock flags in one sync; the last write restored a flag from the initial snapshot. | Stage lock state per story/control in the planner. Both queue orders, combined locking, ancestor protection, refill and save/reopen now pass. |

## Explicit remaining differences

These are documented profile boundaries, not silently successful operations:

- List level formats now batch across different levels and with existing item membership edits. Aliases writing the same level still refuse.
- Detaching a list item and changing paragraph formatting may require separate syncs.
- A write-created proxy requires sync before configuration; read-derived proxies can be used within one sync.
- Lock refusals currently use GeneralException rather than a dedicated protection recovery code.
- Existing comment text is read-only; comment text editing is outside the fixed 81-member profile.
- Advanced tables, floating images, bound/repeating controls, arbitrary fields, and TrackAll remain outside the supported domain.

Both independent consumer workflows pass after the fixes. The independent code
review has no open required-scope finding; see `independent-review.md` for the
reviewer's evidence and the correction to its initial mode probe.
