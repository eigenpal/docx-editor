---
title: 'Custom nodes'
description: 'Define your own inline node types, such as citations, mentions, and merge fields, stored as Word content controls that survive a round trip through Word.'
category: 'Pro'
order: 63
---

A custom node is an inline node type you define: a citation, a mention, a merge field, a clause reference. It is stored as a Word content control (an inline `w:sdt`) whose `w:tag` carries your identity and attributes, so Word opens the document, shows the node's text, and gives it back unchanged.

Requires the custom-nodes module from [`@docx-editor.dev/pro`](/docs/2.x/pro).

## Define one

```tsx
import { defineCustomNode, customNodesModule } from '@docx-editor.dev/pro';

const Citation = defineCustomNode({
  name: 'citation',
  tagPrefix: 'docx',
  label: 'Citation',
  // Host-authored chip appearance. Never file data.
  chrome: { color: '#7c3aed' },
  // Recognition: attrs decoded from the tag, text is the literal control content
  // (which a Word user may have edited, so label drift is visible here).
  fromDocx: ({ attrs, text }) => ({ ...attrs, label: text }),
  // Optional: contribute a card to the review sidebar for every recognized node.
  reviewCard: ({ attrs, text }) => ({
    title: `Citation — ${attrs['sourceId'] ?? 'unknown source'}`,
    detail: text || (attrs['label'] ?? ''),
  }),
});

// Stable identity: module registration is construction-time.
const MODULES = [customNodesModule({ nodes: [Citation] })];
```

| Field                            | Purpose                                                                                           |
| -------------------------------- | ------------------------------------------------------------------------------------------------- |
| `name`                           | Node type name. The second segment of the tag.                                                    |
| `tagPrefix`                      | The prefix this definition claims. `docx` claims `docx:*`. No colons.                             |
| `fromDocx`                       | Recognition hook. Return the attrs the node should carry, or `null` to leave the control literal. |
| `label`                          | Display name for chrome. Defaults to `name`. Localize it yourself.                                |
| `chrome.color`                   | Chip tint and border, applied by `CustomNodeChrome`.                                              |
| `reviewCard`                     | Contribute a sidebar card per node. Return `null` to skip one.                                    |
| `onClick` / `onHover` / `onEdit` | Interaction hooks on the painted chip.                                                            |
| `schema`                         | The shape of the node's payload, as a zod (or valibot, or arktype) schema.                        |
| `payloadNamespace`               | Which customXml store the payloads live in. Defaults to one derived from `tagPrefix`.             |
| `preserveOnExport`               | What happens to this node in a document that leaves the system. See [Export policy](#export-policy). |

Every value reaching `fromDocx` came out of a `.docx`, which is a zip of XML an attacker controls end to end. Treat `attrs` and `text` as untrusted: they are rendered as text, never as markup, and you must not build URLs or DOM from them without sanitizing.

## Render the chips

`CustomNodeChrome` paints the recognized nodes and dispatches interaction. `CustomNodeContextMenu` adds a section on top of the packaged context menu, which is the editing entry point since chips are content-locked by default:

```tsx
import { DocxEditor } from '@docx-editor.dev/react';
import { CustomNodeChrome, CustomNodeContextMenu } from '@docx-editor.dev/pro/react';

export function Editor({ bytes }: { bytes: Uint8Array }) {
  return (
    <DocxEditor.Root document={bytes} modules={MODULES}>
      <DocxEditor.Viewport>
        <DocxEditor.Content />
        <CustomNodeChrome onNodeClick={(node) => openPopover(node)} />
        <DocxEditor.ContextMenu>
          <CustomNodeContextMenu onEditNode={(node) => openEditForm(node)} />
        </DocxEditor.ContextMenu>
      </DocxEditor.Viewport>
    </DocxEditor.Root>
  );
}
```

An activated node gives you `name`, `attrs`, `tag`, `rect` (viewport-relative, for anchoring your own popover), plus `nodeId` and `text` when they resolve.

## Insert, update, remove

These take the editor instance, so reach it with `useDocxEditor()`:

```tsx
import { useDocxEditor } from '@docx-editor.dev/react';
import { insertCustomNode, updateCustomNode, removeCustomNode } from '@docx-editor.dev/pro';

function InsertCitationButton() {
  const editor = useDocxEditor();

  return (
    <button
      disabled={!editor}
      onClick={() => {
        if (!editor) return;
        // At the caret, unless you pass `at: { paragraphId, offset }`.
        const result = insertCustomNode(
          editor,
          Citation,
          { sourceId: 'smith-2024', page: '14' },
          '(Smith 2024, p. 14)',
          { alias: 'Citation' }
        );
        if (!result.ok) console.warn(result.reason);
      }}
    >
      Insert citation
    </button>
  );
}
```

`updateCustomNode(editor, definition, nodeId, attrs, text, options?)` rewrites one in place, and `removeCustomNode(editor, nodeId)` deletes it. All three return an `ExecResult` rather than throwing, so check `ok` and show `reason`.

The default `lock` is `contentLocked`: the text cannot be edited inline, so a label can never drift out of sync with its attributes, while the node stays deletable as one unit in both the editor and Word. Pass `lock: false` for no lock, or `'sdtContentLocked'` to forbid deleting it too.

## Payloads larger than the tag

`w:tag` caps at 64 characters, so it holds a node's identity and nothing else. A citation that
needs authors, a year, a locator and a link needs somewhere else to put them: a **payload**, stored
in a customXml data part the control binds to.

Declare the shape, then pass `data`:

```ts
import { z } from 'zod';

const CitationData = z.object({
  sourceId: z.string().min(1),
  locator: z.string(),
  authors: z.array(z.string()).max(64),
  year: z.number().int(),
  url: z.url().optional(),
});

const Citation = defineCustomNode({
  name: 'citation',
  tagPrefix: 'docx',
  schema: CitationData,
  // `data` arrives already checked against the schema.
  reviewCard: ({ data }) => ({ title: `${data.authors.join(', ')} (${data.year})` }),
});

insertCustomNode(editor, Citation, { sourceId: 'smith-2024' }, '(Smith 2024)', {
  data: { sourceId: 'smith-2024', locator: 'p.14', authors: ['Smith, J.'], year: 2024 },
});
```

The store, the payload and the control are written in **one transaction**: a control bound to a
store that was never written is a document Word offers to repair. A payload that does not match
the schema is refused at the insert, naming the failing field, rather than written and rejected on
the next open.

`schema` is a [Standard Schema](https://standardschema.dev), which zod, valibot and arktype all
implement — bring your own copy, nothing is bundled. Without one, `data` is whatever JSON the file
held, typed `unknown`.

**A bound control is read-only in Word.** Word paints its text from the payload and will not let a
user type into it, so the two can never drift. That is what makes the store the source of truth
rather than a cache of it.

### What happens to a payload

| When                                | What                                                                          |
| ----------------------------------- | ------------------------------------------------------------------------------ |
| `removeCustomNode`                  | The payload goes in the same transaction.                                      |
| A control deleted in Word           | Collected the next time the document opens, by reconciling against what the story still binds. |
| `updateCustomNode` with `data`      | The label and the payload are written together, so they cannot disagree.       |
| A payload that fails its schema     | Reported through `customNodesModule({ onDiagnostic })`. The node still renders, without `data`. |

### Export policy

`preserveOnExport` decides what a node does in a file that leaves your system, and it is a
**separate pipeline from `save()`** — the document at rest keeps its chips so reopening it here
gives them back:

```ts
import { exportCustomNodes } from '@docx-editor.dev/pro';

const bytes = new Uint8Array(await editor.save());
const exported = exportCustomNodes(bytes, [Citation]);
if (exported.ok) download(exported.bytes);
```

| Value            | Effect                                                                        |
| ---------------- | ------------------------------------------------------------------------------ |
| `true` (default) | Untouched: tag, binding and payload all travel.                                |
| `'text'`         | The control is unwrapped. A reader still sees the words; the tag, the binding and the payload are gone. |
| `false`          | The node goes, and takes its content with it.                                  |

Once the last node for a namespace is gone, so are the `customXml` part, its relationship and its
content-type override. A refusal answers no bytes at all, so a caller can never mistake a failed
strip for a successful one.

This removes **this library's markup and nothing else**. A `.docx` also carries its origin in
`docProps/app.xml`, `docProps/core.xml`, comment and revision authors, rsids and custom document
properties — none of which this touches. It does not make a document anonymous.

## Cards in the review sidebar

A definition with `reviewCard` puts one card per node into the review sidebar, anchored at the node's range. `useReviewItem()` scopes host content to the card it is rendered inside, so you can add your own actions to citation cards only:

```tsx
import { DocxEditorReview, useReviewItem } from '@docx-editor.dev/pro/react';

function CitationActions() {
  const item = useReviewItem();
  if (item?.kind !== 'custom') return null;

  return <button onClick={() => openSource(item)}>Open source</button>;
}

<DocxEditorReview>
  <CitationActions />
</DocxEditorReview>;
```

This needs the review module registered alongside the custom-nodes module.

## Server-side authoring

`customNodeXml(...)` builds the same content control as XML, with no editor and no DOM, so a node written on a server is recognized identically when the document opens in the editor:

```ts
import { customNodeXml } from '@docx-editor.dev/pro';

const built = customNodeXml(Citation, { sourceId: 'smith-2024' }, '(Smith 2024)', {
  data: { sourceId: 'smith-2024', locator: 'p.14', authors: ['Smith, J.'], year: 2024 },
});
if (built.ok) {
  template.replace('{{citation}}', built.xml);
  // A template engine can splice markup but cannot author a package, so the parts come back
  // for you to add: `built.store` carries the two customXml parts, both relationships and the
  // content-type override the payload needs.
}
```

`encodeCustomNodeTag` / `decodeCustomNodeTag` are the tag codec if you need to read or write the identity yourself. Tags are capped at `MAX_TAG_LENGTH`.

## Word round-trip

A recognized node is an ordinary inline content control. Word shows its text, honors the lock, and preserves the tag. A document edited in Word and reopened here is recognized again from the same tag. If a Word user edited the text despite the lock, `fromDocx` sees the drift and decides what to do about it.

A document opened without your definition registered renders the control's content literally, which is also what Word does. Nothing is lost.

## Known limit

There is no in-place edit dialog. Re-authoring a node is `removeCustomNode` plus `insertCustomNode`, or `updateCustomNode`, with the form supplied by you. The activation carries `nodeId`, the current `text` and the node's `data` to prefill it.

A bound chip cannot be edited **in Word**, which is what keeps its text and its payload in step. Making one editable there means a two-way binding, which is a per-definition opt-in and is not built.

## Next steps

- [Tracked changes](/docs/2.x/pro/tracked-changes)
- [Comments](/docs/2.x/pro/comments)
- [Content controls](/docs/2.x/guides/content-controls): the built-in control surface these build on
- [Runnable example](https://github.com/eigenpal/docx-editor/tree/main/examples/custom-nodes): define, insert, edit, and round-trip a citation
