---
'@docx-editor.dev/pro': minor
---

Add `useHocuspocusCollaboration` (React and Vue) and `createHocuspocusCollaboration` on new `hocuspocus` subpaths, so a Hocuspocus server room works the same way a WebRTC room does.
