---
'@docx-editor.dev/core': minor
---

Add a format painter: the `format.painter` toolbar slot, `copyFormatting` and `pasteFormatting` commands, `Ctrl+Alt+C` / `Ctrl+Alt+V` (`Command` on macOS), and Copy formatting / Paste formatting rows on the right-click menu. Fixes #519
