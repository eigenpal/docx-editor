---
'@docx-editor.dev/pro': patch
---

Editing a custom node in a header, footer or note keeps its payload. Updating one and naming only its text used to drop the stored data, which then disappeared the next time the document opened.
