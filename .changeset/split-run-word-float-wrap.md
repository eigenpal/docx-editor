---
'@docx-editor.dev/core': patch
---

A word that spans more than one run now wraps to the same place as the same word in one run, so it no longer splits around a floating picture, sits under it, or loses the height of an inline picture on the line it leaves.
