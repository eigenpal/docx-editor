---
'@docx-editor.dev/pro': minor
---

Add `@docx-editor.dev/pro/vue` with the Vue `DocxEditorReview` compound rail, `useReview` and `useReviewAuthor` composables, and author color and avatar styling.
