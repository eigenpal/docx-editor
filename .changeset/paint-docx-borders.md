---
'@eigenpal/docx-editor-core': patch
---

Paint DOCX borders with valid CSS styles, wrap overlong text without overflowing the page, and keep Vue first-page headers, header/footer distances, and page borders in parity with React. Fixes #740
