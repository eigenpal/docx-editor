---
'@docx-editor.dev/pro': minor
---

Collaboration hooks now return a host-facing `CollaborationSession` and typed `CollaborationFailure` errors, and the shared room bootstrap type is `CollaborationBootstrap`.
