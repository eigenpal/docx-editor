---
'@docx-editor.dev/pro': minor
---

Add `useWebrtcCollaboration` so a host can open a WebRTC room without owning its StrictMode lifecycle. Leave keeps the document for local editing, a host collaboration module throws, the paragraph-text factory is `createTextCollaboration`, and the demo signaling list is `DEMO_SIGNALING_ENDPOINTS`.
