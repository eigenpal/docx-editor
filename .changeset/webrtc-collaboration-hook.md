---
'@docx-editor.dev/pro': minor
---

Add `useWebrtcCollaboration` so a host can open a WebRTC room without owning its StrictMode lifecycle. Rename the paragraph-only factory to `createTextCollaboration`; `createYjsCollaboration` remains as a deprecated alias.
