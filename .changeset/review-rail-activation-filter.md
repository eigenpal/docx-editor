---
'@docx-editor.dev/pro': patch
---

The review rail's `structural` and `formatting` filters now also govern which changes a click in the document can activate, so clicking tracked text always opens a card the rail actually shows.
