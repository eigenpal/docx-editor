---
'@docx-editor.dev/core': patch
---

Table rows that cannot break across pages but are taller than a page now start on a new page and continue onto the following pages, instead of failing layout and export.
