---
'@docx-editor.dev/core': patch
---

Press Enter at the end of a paragraph, and the new paragraph takes the style's `w:next` the way Word does, so a heading is followed by body text instead of a second heading.
