---
'@docx-editor.dev/core': minor
---

Add engine-owned history groups, shared React and Vue gesture bindings, and history outcomes so live formatting controls record one undo step per gesture while every value renders and replicates. Fixes #902
