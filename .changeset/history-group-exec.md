---
'@docx-editor.dev/core': minor
---

Add the `historyGroup` option to `Editor.exec` so a live control such as a color picker or a spacing slider records one undo step per gesture while every intermediate value still renders and replicates.
