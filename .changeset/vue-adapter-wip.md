---
'@docx-editor.dev/react': patch
---

The Vue and Nuxt adapters are no longer published while they are rebuilt. `@docx-editor.dev/vue` and `@docx-editor.dev/nuxt` are marked private, so releases skip them; the last published versions stay installable from npm. No change to the React adapter or any other package.
