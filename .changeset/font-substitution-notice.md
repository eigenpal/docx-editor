---
'@docx-editor.dev/react': minor
---

Detect document fonts that render in substitute faces and surface them: `snapshot().fontSubstitutions` lists the families, and the new `DocxEditor.FontNotice` shows a dismissible Word-style compatibility bar (mounted by default in the batteries-included editor).
