---
'@docx-editor.dev/core': patch
---

Hash layout cache tokens and admitted font bytes with the host's native SHA-256 in Node and Bun, which cuts headless export time by about a tenth on long documents. Browsers keep the JavaScript implementation.
