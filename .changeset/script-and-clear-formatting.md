---
'@docx-editor.dev/react': minor
---

Wire superscript, subscript and Clear Formatting. The two script controls are mutually exclusive and carry Word's Ctrl+= and Ctrl+Shift+= shortcuts. Clear Formatting strips direct character formatting from the selection and resets the paragraphs it touches to the default style. Adds the `clearFormatting` editor command.
