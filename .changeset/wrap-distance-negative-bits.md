---
'@docx-editor.dev/core': patch
---

Floating drawings whose text distances are stored as negative values now leave no gap instead of pushing text into a narrow strip or off the page. Changing a drawing's wrap mode keeps the text distances it takes from its anchor.
