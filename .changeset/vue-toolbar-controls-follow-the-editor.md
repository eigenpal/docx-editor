---
'@docx-editor.dev/react': patch
---

Vue toolbar controls now follow the editor instead of a fixed list. Underline, strikethrough, the alignment options and the list and indent buttons run in the Vue toolbar as they already did in React, and a control the editor cannot run right now says why instead of claiming it is unavailable.
