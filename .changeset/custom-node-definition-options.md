---
'@docx-editor.dev/pro': minor
---

`defineCustomNode` takes `schema`, declaring a node's payload shape as a zod (or any Standard Schema) schema, and `preserveOnExport`, declaring whether the node survives a document leaving the system that made it.
