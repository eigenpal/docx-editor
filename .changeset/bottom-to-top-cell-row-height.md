---
'@docx-editor.dev/core': patch
---

Bottom-to-top table cell text now wraps at the height of its row instead of making the row taller, so rotated header rows keep their authored minimum height and paginate correctly.
