---
'@docx-editor.dev/docx-to-pdf': patch
---

PDF export now places each glyph from the unrounded shaped advances, so a glyph's position no longer drifts with the number of glyphs before it on the line.
