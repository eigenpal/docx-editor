---
'@docx-editor.dev/core': patch
---

Give each paste its own bookmark, revision, and `wp:docPr` ids when two people paste into the same document at the same time, so the merged file keeps every marker, tracked change, and drawing addressable on its own.
