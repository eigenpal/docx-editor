---
'@docx-editor.dev/core': minor
---

Word-like copy and paste: copy writes plain text plus HTML with an embedded document fragment, and paste restores styles, lists, tables, links, images, and footnotes — inside the editor and from external HTML. Adds a `pasteWithoutFormatting` command (Ctrl+Shift+V) and an optional `html` payload on `paste`.
