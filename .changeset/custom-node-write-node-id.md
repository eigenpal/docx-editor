---
'@docx-editor.dev/pro': minor
---

Custom node writes now return the id of the control they authored, so a host can follow a node across a rewrite. Clicking a custom node chip also activates reliably when the press repaints the control.
