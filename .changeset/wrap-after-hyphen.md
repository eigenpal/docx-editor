---
'@docx-editor.dev/react': patch
---

Wrap lines after hyphens and dashes the way Word does, so hyphenated words in narrow columns and table cells break instead of overflowing into the neighbouring cell.
