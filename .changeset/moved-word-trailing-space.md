---
'@docx-editor.dev/core': patch
---

A word that wraps to a line or float passage where only the word itself fits now keeps its trailing space at the end of that line, instead of starting the next line with the space or adding a line that holds only the space.
