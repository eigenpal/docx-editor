---
'@docx-editor.dev/core': minor
---

Add an `accept` or `reject` content projection mode to `setTrackedChangesFilter`. Both modes
remain view-only and preserve tracked changes in saved DOCX files.
