---
'@docx-editor.dev/react': patch
---

Paint one element per authored style run per visual line instead of one per word, with spaces and tabs as real painted text. Wrapping, pagination and visible text are unchanged; painted elements drop by 69-88% depending on the document.
