---
'@docx-editor.dev/core': patch
---

A footnote or endnote that holds a content control, a bookmark, a hyperlink or a tracked-change marker is readable and editable again. Such a note used to disappear from the document entirely: its text was not selectable and clicking into it did nothing. Tab also moves between content controls within a header or footer instead of jumping into the document body.
