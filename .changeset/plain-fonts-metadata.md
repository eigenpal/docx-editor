---
'@docx-editor.dev/core': minor
---

Hand a document's declared font metadata to a function-form `resolveFonts`, so a resolver can rank metric-compatible substitutes, and let a substitution carry the requested family's line box through `lineMetrics`.
