---
'@docx-editor.dev/core': minor
---

Let a font substitution carry the requested family's line box through `FontSourceSubstitution.lineMetrics`, so a substitute with different vertical metrics still paginates like the face the document names.
