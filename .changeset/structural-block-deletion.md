---
'@docx-editor.dev/react': minor
---

Deleting a selection that fully contains a table now removes the table instead of emptying it. Select All then Delete on a document with tables used to leave every row, cell and border behind as a blank skeleton.
