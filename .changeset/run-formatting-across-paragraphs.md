---
'@docx-editor.dev/react': patch
---

Apply run formatting across a selection that spans several paragraphs. Bold, italic, underline, strikethrough, font family, font size, colour and highlight now stay enabled and write every paragraph the selection touches instead of greying out at the first paragraph break.
