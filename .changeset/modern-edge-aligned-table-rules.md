---
'@docx-editor.dev/core': minor
---

In documents that use Word 2013 layout, left-aligned and right-aligned tables with an absolute width now place their outer side rule on the aligned edge and measure cell margins from the rule center, so their cells wrap text at the correct width.
