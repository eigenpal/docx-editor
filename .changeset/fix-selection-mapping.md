---
'@eigenpal/docx-editor-core': patch
---

Keep body selection mapping out of footnote position spaces, preserve grapheme boundaries in click fallbacks, retain line-level geometry for virtualized tables, and clip caret/range geometry to the visible body below repeated table headers.
