---
'@docx-editor.dev/pro': minor
---

`defineCustomNode` returns a `CustomNode` with `dataOf`, which narrows a node to that definition and validates its payload against that definition's schema — so a host reads a typed value from `CustomNodeChrome`, the review rail or its own state without writing a parse at the call site.
