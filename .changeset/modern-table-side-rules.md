---
'@docx-editor.dev/core': patch
---

In documents that use Word 2013 layout, centered tables with an absolute width no longer inset cell text by an extra half border width, so their cells wrap text at the correct width.
