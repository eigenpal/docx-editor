---
'@docx-editor.dev/core': patch
---

A vertically merged cell that contains another vertical merge in a different column now sizes each covered row by its own content instead of adding the merged height to its first row.
