---
'@docx-editor.dev/pro': minor
---

Add the `DocxEditorCollaboration` compound to the React and Vue entries: `CaretLabels` renders your own component inside each remote-caret label with full adapter context, and `Avatars`/`Avatar` ship a participant stack whose colors match the review author colors automatically.
