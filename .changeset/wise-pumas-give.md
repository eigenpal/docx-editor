---
'@docx-editor.dev/core': minor
---

Live collaboration now reports every failure a room can reach, including a session that fails after connecting, and presence chrome finds its session through the editor rather than a prop. Adds `DocxEditorCollaborationRoot` for mounting a room and `readCollaborationDocument` for reading one on a server.
