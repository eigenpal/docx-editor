---
'@docx-editor.dev/pro': minor
---

Add `useDocumentCollaboration` and `useCollaborationParticipants` for React and Vue, expose `ydoc` and `provider` plus a `rejoin` recovery on the WebRTC hooks, and add `session.setIdentity` for live name and color changes. `leave` now requires the saved document bytes so nothing typed in the room is lost.
