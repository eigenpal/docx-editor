---
'@docx-editor.dev/react': minor
---

Render embedded SVG images instead of showing an "Unsupported image format (SVG)" placeholder. SVG paints at the authored size in the browser's secure static mode, so scripts and external references inside the file stay inert. Inserting a new SVG is still not supported.
