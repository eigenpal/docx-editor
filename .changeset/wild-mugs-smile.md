---
'@docx-editor.dev/core': patch
---

Resolve the header and footer variant per page, so a title page with no first-page header starts its body at the top margin, and evaluate the `\#` numeric picture switch on `PAGE` and `NUMPAGES` fields.
