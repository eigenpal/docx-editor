---
'@docx-editor.dev/core': patch
---

A vertical table rule from `w:insideV` now sits centred on its column boundary in the first and last columns too, instead of only where the cell owns both side edges.
