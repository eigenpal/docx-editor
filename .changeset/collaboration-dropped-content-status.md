---
'@docx-editor.dev/pro': patch
---

Report a collaboration status of `error` when shared state can only be materialized by leaving content out, instead of repairing the document silently while the session still reads `ready`.
