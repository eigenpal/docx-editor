---
'@docx-editor.dev/pro': patch
---

A collaboration session that cannot accept the document no longer throws out of the editor's mount path, so the editor stays mounted and reports that it is out of sync instead of going blank.
