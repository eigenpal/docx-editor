---
'@docx-editor.dev/core': patch
---

A `w:lineRule="atLeast"` line taller than its text now grows upward, so the text sits on the line's floor. PDF text baselines are also placed more consistently.
