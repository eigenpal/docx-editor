---
'@docx-editor.dev/core': patch
---

A `w:lineRule="atLeast"` line taller than its text now grows upward, so the text sits on the line's floor, and a PDF baseline that lands on an exact half of the device grid rounds toward the top of the page.
