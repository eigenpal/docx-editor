---
'@docx-editor.dev/core': patch
---

Footnotes referenced from table rows now stay on the page of their row. A long footnote continues on the next page when it can place at least two lines below the whole row, including rows that cannot split; otherwise the row moves to the next page with the footnote.
