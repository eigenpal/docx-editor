---
'@docx-editor.dev/react': patch
---

Balance the columns of a multi-column section that ends in a continuous section break, matching Word's even split instead of filling the first column to the page bottom.
