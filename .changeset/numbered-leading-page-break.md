---
'@docx-editor.dev/core': patch
---

A numbered or bulleted paragraph that starts with a manual page break keeps its list number or bullet with its text on the next page, and no longer adds a page that holds only the number.
