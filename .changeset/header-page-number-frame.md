---
'@docx-editor.dev/core': patch
---

A page number frame at the start of a header shares the first line of the header text instead of adding a line, so the body starts one line higher. Frames aligned inside or outside alternate margins by physical page, also after a page numbering restart.
